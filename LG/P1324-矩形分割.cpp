#include <algorithm>
#include <iostream>
#include <cstring>
#include <climits>
#include <cstdlib>
#include <cstdio>
#include <cmath>
#include <queue>
#include <stack>
#include <map>
#include <set>

const int MAXN = 2000 + 5;
int N,M;
int l1[MAXN],l2[MAXN];

inline bool cmp(){
	
}

int main(){
	scanf("%d%d",&N,&M);
	for(int i = 1;i < N;i++)
		scanf("%d",&l1[i]);
	for(int i = 1;i < M;i++)
		scanf("%d",&l2[i]);

	return 0;
}